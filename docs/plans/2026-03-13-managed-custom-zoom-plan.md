# 2026-03-13 Managed Custom Zoom Plan

1. Add pure custom-zoom tracker model and tests for toggle semantics.
2. Integrate stable window identity and operation planning into the macOS clamp adapter.
3. Keep fallback geometric clamp behavior for non-zoomed windows.
4. Verify with `cargo check` and focused window clamper tests after each step.
